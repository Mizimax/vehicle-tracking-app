import React from "react";
import styled from "styled-components";

const BoxWrapper = styled.div`
  
`

const VehicleDetailBox: React.FC<any> = ({vehicleData}) => {
  return (
    <BoxWrapper>

    </BoxWrapper>
  );
};

export default VehicleDetailBox;
